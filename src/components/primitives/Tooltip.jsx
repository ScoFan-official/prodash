// Placeholder until Task 3 replaces this with the Radix Tooltip implementation.
// Renders children as-is; IconButton currently has no tooltip behavior.
export default function Tooltip({ children, content }) {
  return children;
}
